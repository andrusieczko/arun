define([], function() {
  'use strict';
  describe('App', function() {
    it('should be truthy', function() {
      // given
      var expected = 1;

      // when
      var result = 1;

      // then
      expect(result).toBe(expected);
    });
  });
});